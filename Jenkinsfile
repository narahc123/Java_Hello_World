pipeline {
   
  agent {
    docker {
        image 'openjdk'
    }
  }
     stages {
        stage('Example Build') {
            steps {
                sh 'java -version'
            }
        }
       
     }
    
}
