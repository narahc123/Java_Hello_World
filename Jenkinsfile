pipeline {
   
  agent {
    docker {
        image 'jenkins/jenkins'
    }
  }
     stages {
        stage('Example Build') {
            steps {
                sh 'java -version'
            }
        }
       
     }
    
}
